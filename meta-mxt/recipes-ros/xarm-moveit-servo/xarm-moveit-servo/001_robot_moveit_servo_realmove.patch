diff --git a/xarm_moveit_servo/launch/_robot_moveit_servo_realmove.launch.py b/xarm_moveit_servo/launch/_robot_moveit_servo_realmove.launch.py
index bf1315f..2496230 100644
--- a/xarm_moveit_servo/launch/_robot_moveit_servo_realmove.launch.py
+++ b/xarm_moveit_servo/launch/_robot_moveit_servo_realmove.launch.py
@@ -139,21 +139,21 @@ def launch_setup(context, *args, **kwargs):
     controllers = ['joint_state_broadcaster']
 
     # rviz_config_file = PathJoinSubstitution([FindPackageShare(moveit_config_package_name), 'rviz', 'moveit.rviz'])
-    rviz_config_file = PathJoinSubstitution([FindPackageShare('xarm_moveit_servo'), 'rviz', 'servo.rviz'])
-    rviz_node = Node(
-        package='rviz2',
-        executable='rviz2',
-        name='rviz2',
-        output='screen',
-        arguments=['-d', rviz_config_file],
-        parameters=[
-            robot_description_parameters,
-        ],
-        remappings=[
-            ('/tf', 'tf'),
-            ('/tf_static', 'tf_static'),
-        ]
-    )
+    #rviz_config_file = PathJoinSubstitution([FindPackageShare('xarm_moveit_servo'), 'rviz', 'servo.rviz'])
+    #rviz_node = Node(
+    #    package='rviz2',
+    #    executable='rviz2',
+    #    name='rviz2',
+    #    output='screen',
+    #    arguments=['-d', rviz_config_file],
+    #    parameters=[
+    #        robot_description_parameters,
+    #    ],
+    #    remappings=[
+    #        ('/tf', 'tf'),
+    #        ('/tf_static', 'tf_static'),
+    #    ]
+    #)
 
     # ros2 control launch
     # xarm_controller/launch/_ros2_control.launch.py
@@ -263,7 +263,7 @@ def launch_setup(context, *args, **kwargs):
                 on_exit=container,
             )
         ),
-        rviz_node,
+        #rviz_node,
         joint_state_publisher_node,
         ros2_control_launch,
         traj_controller_node,
