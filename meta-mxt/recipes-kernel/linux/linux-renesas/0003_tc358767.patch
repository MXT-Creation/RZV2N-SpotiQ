diff --git a/drivers/gpu/drm/bridge/tc358767.c b/drivers/gpu/drm/bridge/tc358767.c
index 926ab5c3c..266f29c39 100644
--- a/drivers/gpu/drm/bridge/tc358767.c
+++ b/drivers/gpu/drm/bridge/tc358767.c
@@ -697,7 +697,7 @@ static int tc_aux_link_setup(struct tc_data *tc)
 
 	ret = tc_poll_timeout(tc, DP_PHY_CTRL, PHY_RDY, PHY_RDY, 100, 100000);
 	if (ret == -ETIMEDOUT) {
-		dev_err(tc->dev, "Timeout waiting for PHY to become ready");
+		dev_info(tc->dev, "Timeout waiting for PHY to become ready");
 		return ret;
 	} else if (ret) {
 		goto err;
@@ -720,7 +720,7 @@ static int tc_aux_link_setup(struct tc_data *tc)
 
 	return 0;
 err:
-	dev_err(tc->dev, "tc_aux_link_setup failed: %d\n", ret);
+	dev_info(tc->dev, "tc_aux_link_setup failed: %d\n", ret);
 	return ret;
 }
 
@@ -1579,6 +1579,14 @@ static struct edid *tc_get_edid(struct drm_bridge *bridge,
 				struct drm_connector *connector)
 {
 	struct tc_data *tc = bridge_to_tc(bridge);
+	
+	int ret;
+
+	ret = tc_get_display_props(tc);
+	if (ret < 0) {
+		dev_err(tc->dev, "failed to read display props: %d\n", ret);
+		return 0;
+	}
 
 	return drm_get_edid(connector, &tc->aux.ddc);
 }
@@ -1899,7 +1907,8 @@ static int tc_mipi_dsi_host_attach(struct tc_data *tc)
 
 	dsi->lanes = dsi_lanes;
 	dsi->format = MIPI_DSI_FMT_RGB888;
-	dsi->mode_flags = MIPI_DSI_MODE_VIDEO | MIPI_DSI_MODE_VIDEO_SYNC_PULSE;
+	dsi->mode_flags = MIPI_DSI_MODE_VIDEO | MIPI_DSI_MODE_VIDEO_BURST |
+	                  MIPI_DSI_MODE_LPM | MIPI_DSI_CLOCK_NON_CONTINUOUS;
 
 	ret = devm_mipi_dsi_attach(dev, dsi);
 	if (ret < 0) {
@@ -1943,10 +1952,11 @@ static int tc_probe_edp_bridge_endpoint(struct tc_data *tc)
 {
 	struct device *dev = tc->dev;
 	struct drm_panel *panel;
+	struct drm_bridge *bridge;
 	int ret;
 
 	/* port@2 is the output port */
-	ret = drm_of_find_panel_or_bridge(dev->of_node, 2, 0, &panel, NULL);
+	ret = drm_of_find_panel_or_bridge(dev->of_node, 2, 0, &panel, &bridge);
 	if (ret && ret != -ENODEV)
 		return ret;
 
@@ -2081,6 +2091,8 @@ static int tc_probe(struct i2c_client *client, const struct i2c_device_id *id)
 	if (tc->reset_gpio) {
 		gpiod_set_value_cansleep(tc->reset_gpio, 1);
 		usleep_range(5000, 10000);
+		gpiod_set_value_cansleep(tc->reset_gpio, 0);
+		usleep_range(5000, 10000);
 	}
 
 	tc->regmap = devm_regmap_init_i2c(client, &tc_regmap_config);
@@ -2177,7 +2189,7 @@ static int tc_probe(struct i2c_client *client, const struct i2c_device_id *id)
 		ret = tc_mipi_dsi_host_attach(tc);
 		if (ret) {
 			drm_bridge_remove(&tc->bridge);
-			return ret;
+			return dev_err_probe(dev, ret, "Failed to attach DSI host\n");
 		}
 	}
 
